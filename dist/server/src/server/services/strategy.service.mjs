import{env as e,importAll as t}from"@sidekick-coder/zenith-kit/server";import n from"path";import{LoggerService as r}from"@sidekick-coder/zenith-kit/shared";var i=class{items=[];debug=!1;logger;constructor(e={}){this.debug=e.debug||!1,this.logger=e.logger||new r,this.debug&&this.logger.debug(`initialized in debug mode`)}async load(){this.items=[];let r=await t(e.production?n.resolve(import.meta.dirname,`../../../strategies`):n.resolve(import.meta.dirname,`../strategies`),{exclude:[`BaseStrategy`]});for(let e of Object.values(r)){let t=e.default;this.items.push({id:t.id,label:t.label,description:t.description,ctor:t}),this.debug&&this.logger.debug(`loaded strategy: ${t.id}`)}this.debug&&this.logger.debug(`loaded ${this.items.length} strategies`,{strategies:this.items.map(e=>e.id)})}async list(){return this.items}async find(e){let t=(await this.list()).find(t=>t.id===e);if(!t)throw Error(`Strategy not found`);return t}async instantiate(e){return new(await(this.find(e.strategy)))(e.config,e)}};export{i as default};