import{LoggerService as e}from"@sidekick-coder/zenith-kit/shared";var t=class{static id=`base`;static label=`Base Strategy`;static description=`This is the base strategy class.`;plan;debug=!1;logger;constructor(t){this.plan=t.plan,this.logger=t.logger||new e,this.debug=t.debug||!1,this.debug&&this.logger.debug(`initialized strategy ${this.constructor.name} with plan ID ${this.plan.id}`)}get config(){return this.plan.config||{}}get triggers(){return this.plan.triggers||[]}async execute(e={}){}};export{t as default};