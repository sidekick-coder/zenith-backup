import e from"./DumpStrategy.mjs";import t from"path";var n=class extends e{static id=`dump_sqlite`;static label=`Dump SQLite Strategy`;static description=`A dump strategy specifically designed for SQLite databases, utilizing the sqlite3 command-line tool to create dumps. It ensures that the database is properly locked during the dump process to maintain data integrity.`;constructor(e){super(e);let n=this.config.sqlite_filename,r=n?t.basename(n):`database.sqlite`,i=n?t.dirname(n):``;this.docker_image=`nouchka/sqlite3:latest`,this.docker_args=[`-v`,`${i}:/data:rw`],this.backup_command=`/data/${r} '.backup {{output}}'`,this.backup_filename=`database.dump`,this.restore_command=`/data/${r} '.restore {{input}}'`}};export{n as default};